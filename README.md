# CIT 386 - Assignment 2.2

**Student:** Matthew Espinel

## Virtual Machine Export

This repository contains verification information for my exported Ubuntu virtual machine used in CIT 386 Assignment 2.2.

The virtual machine was exported from VirtualBox as:

`matthew-cit386.ova`

Because the OVA file is larger than GitHub's browser upload limit, the full virtual machine file is not stored directly in this repository.

## SHA256 Verification

A SHA256 checksum was generated for the exported OVA file using Windows PowerShell.

The verification information is included in:

`checksum.txt`

This checksum can be used to verify the integrity of the exported virtual machine file.

## Assignment Work

The assignment included:
- Linux command-line administration
- File and directory management
- Permissions
- System and network commands
- Connectivity testing
- VirtualBox virtual machine management
- VM snapshot creation
- OVA export and SHA256 verification
